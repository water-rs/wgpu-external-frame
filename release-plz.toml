[workspace]
pr_draft = false
pr_labels = ["release"]
git_release_name = "v{{ version }}"
publish = true
release_always = true
git_tag_enable = true
changelog_update = true

[[package]]
name = "wgpu-external-frame"
git_release_name = "v{{ version }}"
git_tag_name = "v{{ version }}"
